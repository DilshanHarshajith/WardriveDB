services:
  wardrivedb:
    build: .
    ports:
      - "${PORT:-8765}:${PORT:-8765}"
    environment:
      - PORT=${PORT:-8765}
      - WARDRIVING_DB=${WARDRIVING_DB:-}
    volumes:
      - ${DATA_DIR:-.}:/data
    read_only: true